import { Component } from "solid-js";

import FakeChat from "./FakeChat";

const WysiwygConfigInput: Component<{}> = (props) => {
  return (
    <div>
      <FakeChat />
    </div>
  );
};

export default WysiwygConfigInput;
